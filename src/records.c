#include <esptools/records.h>

struct esp_field *esp_record_next_field(struct esp_record const *rec,
                                        struct esp_field const *prv_field, uint32_t* field_size)
{
}